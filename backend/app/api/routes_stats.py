"""routes_stats — SPEC 7.1. Mounted in a later phase (DECISIONS.md D-012).

Phase 0 keeps this as an unmounted stub.
"""

from fastapi import APIRouter

router = APIRouter()

# TODO(phase-1..4): implement endpoints per SPEC 7.1.
