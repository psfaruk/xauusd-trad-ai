# Frontend environment (SPEC §11). Copy to .env — NEVER commit the real .env.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Leave empty in dev: requests go same-origin through the Vite proxy (D-005).
VITE_API_URL=
VITE_WS_URL=
